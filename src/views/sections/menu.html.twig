{% block menu %}

    {% if menu %}
        <ul {% if classList is not empty %}class="{{ classList }}"{% endif %} >
        {% for menu_item in menu %}
            <li><a href="{{ menu_item.link }}">{{ menu_item.title }}</a></li>
        {% endfor %}
        </ul>
    {% endif %}
    
{% endblock %}
